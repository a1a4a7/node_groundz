version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - yum update -y
      - yum install -y jq
  pre_build:
    commands:
      - echo "Logging in to Docker Hub..."
      - echo "Ydckr_pat_bEdD4_1FV6l60lQ_G-PBUmoHVZU" | docker login --username kitho147@gmail.com --password-stdin
  build:
    commands:
      - echo "Building Docker image..."
      - docker-compose up --build -d
  post_build:
    commands:
      - echo "Build complete."
artifacts:
  files:
    - '**/*'
