version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies..."
      - yum update -y
      - yum install -y jq
  pre_build:
    commands:
      - echo "Retrieving DockerHub credentials from Secrets Manager..."
      - aws secretsmanager get-secret-value --secret-id dockerhub-auth --region us-east-1 --query 'SecretString' --output text > dockerhub-credentials.json
      - export DOCKER_USERNAME=$(cat dockerhub-credentials.json | jq -r '.username')
      - export DOCKER_PASSWORD=$(cat dockerhub-credentials.json | jq -r '.password')
      - echo $DOCKER_USERNAME
      - echo $DOCKER_PASSWORD
      - echo $DOCKER_PASSWORD | docker login --username $DOCKER_USERNAME --password-stdin
  build:
    commands:
      - echo "Building Docker image..."
      - docker-compose up --build -d
  post_build:
    commands:
      - echo "Build complete."
artifacts:
  files:
    - '**/*'
